# Installation

To install **Scout**, make sure you have the latest stable dist available. To use a generate a distribution from this repository,

```bash
pipenv run setup.py sdist 
```

Then simply install the current version :

```bash
pipenv install /path/to/dist/scout-0.1.tar.gz 
```